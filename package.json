{
  "name": "infected-voices",
  "version": "0.7.0",
  "private": true,
  "description": "Infected Voices Studio for the browser, Windows, iPhone, iPad, and Android",
  "workspaces": [
    "studio"
  ],
  "scripts": {
    "dev": "npm run dev -w infected-voices-studio",
    "build:web": "node scripts/build-web.mjs",
    "test": "node --experimental-strip-types --test studio/src/lib/studioFeatures.test.ts",
    "preview": "npm run preview -w infected-voices-studio -- --host",
    "package:windows": "npm run build:web && node scripts/package-windows.mjs",
    "native:ios": "npm run build:web && node scripts/prepare-native.mjs ios",
    "sync:ios": "npm run build:web && npx cap sync ios && node scripts/patch-native.mjs ios",
    "open:ios": "npm run sync:ios && npx cap open ios",
    "verify:web": "node scripts/verify-web.mjs",
    "native:android": "npm run build:web && node scripts/prepare-native.mjs android",
    "sync:android": "npm run build:web && npx cap sync android && node scripts/patch-native.mjs android",
    "open:android": "npm run sync:android && npx cap open android"
  },
  "dependencies": {
    "@aparajita/capacitor-secure-storage": "8.0.0",
    "@breezystack/lamejs": "1.2.7",
    "@capacitor/app": "8.0.0",
    "@capacitor/browser": "8.0.0",
    "@capacitor/core": "8.0.0",
    "@capacitor/filesystem": "8.0.0",
    "@capacitor/haptics": "8.0.0",
    "@capacitor/ios": "8.0.0",
    "@capacitor/network": "8.0.0",
    "@capacitor/preferences": "8.0.0",
    "@capacitor/share": "8.0.0",
    "@capacitor/android": "8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.0.0",
    "esbuild": "0.25.9"
  }
}
