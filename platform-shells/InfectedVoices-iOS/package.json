{
  "name": "infectedvoices-ios",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Thin Capacitor iOS shell. App Store upload is not run from the populate step.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test scripts/self-check.test.mjs",
    "check": "node scripts/prepare-ios.mjs --check",
    "sync": "node scripts/prepare-ios.mjs --sync"
  }
}
